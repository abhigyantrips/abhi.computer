---
title: My Cloudflare WAF Rules for Domains
description: This is how I tackle malicious (but harmless and annoying) requests being made to my domains on Cloudflare.
date: 2026-06-14
tech:
  - cloudflare
fields:
  - web-security
  - infrastructure
---

I've been a happy Cloudflare freeloader[^1] for about a year now. If you've ever tracked requests going through your domains (through Observability or otherwise), you have probably seen requests similar to these.

![A list of requests captured by Cloudflare Observability.](./images/my-cloudflare-waf-rules-for-domains-1.png)

In my case -- a static [Astro](https://astro.build/) site deployed on Cloudflare -- all of these requests will return a 404. But they are still *hits*, and will still be counted towards the Worker's [billing cycle of 100K requests per day](https://developers.cloudflare.com/workers/platform/pricing/#workers). It's a lot of dead traffic from some poor bot trying to fingerprint your site.

Cloudflare, funnily enough, already provides a pretty good solution with [WAF Custom Rules](https://developers.cloudflare.com/waf/custom-rules/). Deploying a Worker on a custom domain requires the domain to be onboarded onto Cloudflare, and even the Free Tier for domains provides upto 5 Custom WAF Rules you can add


```
(http.request.uri.path wildcard r"*/.env*") or
(http.request.uri.path wildcard r"*/.git*") or
(http.request.uri.path wildcard r"*/.svn*") or
(http.request.uri.path wildcard r"*/.htaccess*") or
(http.request.uri.path wildcard r"*/web.config*") or
(http.request.uri.path wildcard r"*/.aws*") or
(http.request.uri.path wildcard r"*/.ssh*") or
(http.request.uri.path wildcard r"*/wp-*") or
(http.request.uri.path wildcard r"*/xmlrpc*") or
(http.request.uri.path wildcard r"*/phpmyadmin*") or
(http.request.uri.path wildcard r"*/phpinfo*") or
(http.request.uri.path wildcard r"*.php") or
(http.request.uri.path wildcard r"*.yml") or
(http.request.uri.path wildcard r"*.yaml") or
(http.request.uri.path wildcard r"*.toml") or
(http.request.uri.path wildcard r"*.ini") or
(http.request.uri.path wildcard r"*.bak") or
(http.request.uri.path wildcard r"*.sql") or
(http.request.uri.path wildcard r"*.tar*") or
(http.request.uri.path wildcard r"*.zip")
```

```ts title="console.ts"
console.log("Hello, world!");
```

[^1]: And a "payloader", I have some Workers running on the Paid plan. :P
